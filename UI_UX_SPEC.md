# UI/UX 기획서

## 1. UX 목표

- 캠프에서 첫 출전까지 3번 이내의 주요 선택으로 도달한다.
- 정보량이 많아도 중앙 전투 시야를 보존한다.
- 주요 버튼은 모바일 가로 화면에서 엄지로 누르기 쉽다.
- 캐릭터와 전리품의 희귀도·성장 상태를 즉시 파악할 수 있다.
- 기본 Material UI가 아니라 일관된 다크 판타지 게임 UI로 보인다.

## 2. 기준 화면과 반응형 전략

### 기본 설계 캔버스

- 기준: 1280×720 가로
- 최소 지원 가로: 800×450
- 태블릿/데스크톱: 16:9 중심, 여분 공간을 배경과 패널 여백으로 사용
- iOS/Android 세로 화면: 앱과 네이티브 설정에서 양쪽 가로 방향으로 고정
- Web 세로/좁은 창: 방향을 강제하지 않고 가로 회전 또는 창 확장 안내를 제공
- SafeArea: 노치, 상태 표시줄, 제스처 영역을 항상 반영

### 안전 영역

- 상단 HUD: 화면 높이의 최대 14%
- 하단 스킬/무기 HUD: 화면 높이의 최대 16%
- 좌측 목표 패널: 화면 너비의 최대 22%
- 우측 행동 패널: 화면 너비의 최대 18%
- 중앙 전투 안전 영역: 화면 너비 56%, 높이 66% 이상

### 전투 조작 영역

- 왼쪽 아래: 76×76 가상 스틱, 손가락 이동 반경 24px, 해제 시 즉시 중앙 복귀
- 오른쪽 아래: 대시 → 전장 특별 행동 → 현재 빌드 → 궁극기 순서
- 900px 미만 가로에서는 현재 빌드 아이콘을 최대 2개로 줄이고 대시·특별 행동·궁극기를 항상 보존
- 대시와 특별 행동은 원형 진행 테두리, 남은 초, 활성 금색 테두리를 함께 사용
- 레벨업/사건/일시정지 오버레이는 모든 전투 입력보다 위에서 포인터를 차단

## 3. 디자인 토큰

### 간격

4px 단위를 기본으로 사용한다.

- xs: 4
- sm: 8
- md: 12
- lg: 16
- xl: 24
- xxl: 32

### 터치 영역

- 일반 버튼 최소 높이 48px
- 아이콘 버튼 최소 44×44px
- 전투 특별 행동 최소 52×52px
- 요소 사이 오작동 방지 간격 최소 8px

### 타이포 계층

| 용도 | 크기 | 굵기 | 색 |
|---|---:|---:|---|
| 화면 제목 | 20~24 | 700 | 주요 텍스트 |
| 결과/레벨업 제목 | 28~42 | 800 | 금색 |
| 패널 제목 | 14~18 | 700 | 주요 텍스트 |
| 본문 | 12~14 | 400~500 | 주요/보조 텍스트 |
| 설명/메타 | 10~12 | 400 | 보조 텍스트 |
| 숫자 강조 | 14~22 | 700 | 금색/상태색 |

한글은 Noto Sans KR 계열을 기본으로 하고 영문 장식 제목은 serif 또는 대문자 letter spacing을 제한적으로 사용한다.

### 패널

- 배경 불투명도 88~96%
- 일반 1px 황동 테두리
- 선택/전설 2px 금색 또는 대표색 테두리
- 그림자는 검은색 저강도, blur 8~16px
- 코너 반경은 0~4px로 제한해 금속 판 느낌 유지

## 4. 공통 컴포넌트

### FantasyButton

상태:

- 기본: 짙은 갈색
- 주요 행동: 남색
- 눌림: 밝기 8% 감소, 테두리 안쪽 이동
- 비활성: 채도 50% 감소, 이유 툴팁/문구 제공
- 위험 행동: 적갈색, 확인이 필요한 경우 별도 다이얼로그

버튼은 `아이콘 + 동사 중심 라벨` 구조를 사용한다. `확인`보다 `계약 수락`, `장착`, `귀환`처럼 결과가 명확한 문구를 사용한다.

### GoldPanel

- 콘텐츠 계층을 묶는 기본 컨테이너
- 내부 패딩 최소 12px
- 스크롤 영역과 고정 행동 영역을 시각적으로 분리
- 선택 가능한 패널은 hover/focus/pressed 상태 제공

### CharacterCard

- 초상, 이름, 레벨, 등급, 종족, 직업을 표시
- 선택 상태는 금색 테두리와 낮은 보라 그림자
- 잠김 상태는 실루엣, 해금 조건, 클릭 피드백 제공
- 작은 카드에서 능력치 본문을 과도하게 표시하지 않는다.

### WeaponCard

- 무기 아이콘, 이름, 등급, 공격력
- 고유장비 배지
- 장착 상태는 테두리와 체크/`장착 중` 라벨을 함께 사용
- 소유하지 않은 장비는 획득처를 표시

### Currency

- 아이콘, 수량, 추가 버튼의 순서
- 천 단위 구분 적용
- 수량 변경 시 짧은 증감 애니메이션
- 재화가 부족하면 필요한 양과 획득 경로를 안내

## 5. 전체 내비게이션

```text
캠프
├── 전쟁 계약
│   └── 출전 용병 선택
│       ├── 장비 변경
│       └── 전투
│           ├── 레벨업
│           ├── 전장 사건 선택
│           └── 결과
│               ├── 캠프 귀환
│               └── 재출전
├── 용병 명부
│   └── 용병 상세
│       ├── 정보
│       ├── 레벨업
│       ├── 장비
│       ├── 스킬
│       └── 스토리
├── 장비
├── 모집
├── 상점
├── 임무
├── 도감
└── 대장간
```

뒤로가기는 현재 편집 상태를 보존하되, 재화 소비 또는 장비 분해처럼 되돌리기 어려운 변경이 있으면 확인한다.

## 6. 화면별 명세

### S01. 용병단 캠프

목적: 모든 메타 기능의 허브이자 용병단이 실제로 생활하는 장소를 보여준다.

구성:

- 상단: 단장 정보, 골드, 유료/희귀 재화, 우편, 설정
- 좌측: 용병, 장비, 상점, 임무, 도감
- 중앙: 캠프 배경과 상태 변화/NPC
- 우측: 전쟁터 출전, 용병 모집, 대장간
- 하단: 오늘의 전쟁 보상과 갱신 상태

상호작용:

- 알림이 있는 메뉴는 작은 적색 배지를 표시한다.
- 주요 버튼 `전쟁터 출전`은 가장 높은 대비를 사용한다.
- 캠프 배경은 시간대, 새 용병, 시설 성장에 따라 변형 가능해야 한다.
- 지휘 천막, 대장간, 용병 길드, 상인, 의무소는 배경 위 금속 테두리 핫스팟으로 표시하고 관련 전용 화면으로 바로 이동한다.
- 상단 상황판은 마지막 계약의 승리/철수/패배, 부상자 수, 현재 시간대와 날씨만 한 줄로 요약한다.

상태:

- 신규 사용자: 출전 버튼과 안내만 강조
- 보상 대기: 해당 메뉴 배지와 NPC 말풍선
- 네트워크/저장 오류: 상단 비차단 배너와 재시도

### S02. 전쟁 계약 선택

목적: 위험, 시간, 보상, 세력 조건을 비교해 계약을 고른다.

구성:

- 상단: 화면 제목, 갱신 횟수/남은 시간
- 중앙: 전쟁 지도와 계약 마커
- 하단: 선택 계약 요약과 계약 수락 버튼
- 필터: 일반/특수/희귀/전설, 세력, 전투 유형

계약 마커:

- 선택 전: 아이콘, 계약 이름, 난이도 색
- 선택 후: 크기 8% 확대, 금색 링, 연결 지역 강조
- 잠김: 잠금 아이콘과 조건
- 하단 요약: 고용 세력, 관계 등급, 현재 평판, 보상 성향과 3단계 작전 진행도를 함께 표시한다.

### S03. 출전 용병 선택

목적: 계약에 맞는 용병과 장비를 최종 확정한다.

구성:

- 좌측: 보유 용병 목록
- 우측: 선택 용병 전신, 전투력, 특성, 장착 무기, 궁극기
- 하단: 장비 변경, 이 용병으로 출전

규칙:

- 용병 변경 시 기본적으로 마지막 장착 무기를 유지한다.
- 권장 전투력 미달은 출전을 막지 않고 위험 경고를 표시한다.
- 계약 특수 조건과 시너지가 있으면 녹색/금색 문구로 표시한다.

### S04. 장비/무기

목적: 현재 용병의 공격 방식과 궁극기 조합을 선택한다.

구성:

- 좌측/중앙: 무기 그리드와 필터
- 우측: 무기 이미지, 능력치, 특성, 성장 단계, 착용자
- 하단: 장착/강화/획득처 행동

상태:

- 고유장비 + 소유자: `궁극기 활성화`
- 고유장비 + 다른 용병: 일반 특성만 표시, 궁극기 비활성 이유 제공
- 미보유: 실루엣과 획득처
- 장착 중: 명확한 라벨과 비활성 장착 버튼

### S05. 용병 명부

목적: 보유 캐릭터를 비교하고 성장 대상을 찾는다.

구성:

- 상단: 보유 수, 정렬, 필터
- 그리드: 초상, 이름, 레벨, 등급, 종족/직업 아이콘
- 필터: 종족, 직업, 등급, 레벨, 속성
- 정렬: 전투력, 레벨, 등급, 최근 획득

필터 결과가 없으면 조건 초기화 버튼을 제공한다.

### S06. 용병 상세

목적: 캐릭터 수집 가치, 현재 성장 상태, 다음 행동을 한 화면에서 제공한다.

구성:

- 왼쪽/중앙: 고품질 전신 일러스트
- 오른쪽: 이름, 등급, 종족, 직업, 레벨, 전투력, 능력치
- 탭: 정보, 레벨업, 장비, 스킬, 스토리
- 고유특성과 고유무기 카드

일러스트는 UI보다 한 단계 뒤에 놓되 얼굴, 귀, 대표 무기가 가려지지 않게 한다.

레벨업 탭은 현재/상한 레벨, 승급 단계, 경험치 게이지와 보유 골드·야전 식량·계약 인장을 한 화면에 배치한다. 훈련과 승급 버튼 아래에는 비용과 조건을 항상 노출하고 실패 시 같은 패널 상단에 비차단 안내를 표시한다. 장비 탭은 현재 장착 무기의 영구 레벨/성장 단계와 고유무기 공명을 표시한 뒤 장비 변경 화면으로 연결한다.

### S06-1. 대장간과 임무

- 대장간 좌측은 8개 무기의 영구 레벨/단계를 비교하며 우측은 강화, 제작/분해, 전리품 도감 3개 탭을 사용한다.
- 강화는 전/후 XP와 소모 재화를 행동 전에 읽을 수 있어야 하며 완료 결과는 패널 상단 금색 메시지로 즉시 확인한다.
- 전리품 도감은 아이템 이름, 주요 획득처, 현재 보유 수량을 같은 행에 배치한다.
- 임무 카드는 조건, 보상, 진행 중/보상 수령/수령 완료 3상태를 색과 아이콘·문구로 함께 구분한다.
- 수령 가능한 임무가 있으면 캠프 좌측 임무 메뉴에 적색 알림점을 표시하고 수령 후 즉시 재계산한다.

### S07. 전투

목적: 이동과 판단에 집중하면서 전황, 목표, 빌드 상태를 빠르게 읽는다.

상단:

- 좌상단: 용병 초상, 이름, 런 레벨, HP, 경험치
- 중앙: 남은 시간/목표 단계
- 우상단: 미니맵, 일시정지

좌측:

- 현재 목표 1~3개와 진행률
- 목표 월드 마커 방향
- 성문 현재/최대 HP, 색상 변화 게이지와 전선 침투율

하단:

- 좌측 가상 스틱
- 우측 무기/스킬/궁극기
- 중앙은 비운다.

궁극기 오브는 고유무기 미장착 시 `LOCK`, 충전 중에는 백분율, 완충 시 금색 `READY`를 표시한다. 발동 컷인은 0.72초 안에 캐릭터 전신, 궁극기명, 이명을 읽을 수 있게 진입하고 전체 시퀀스는 약 2.4초를 넘지 않는다. 우상단 VFX 토글은 저사양 모드의 켜짐/꺼짐을 즉시 아이콘으로 구분한다.

일반 피해 숫자는 작은 따뜻한 백색, 치명타는 더 큰 금색으로 0.72초만 표시하며 동시 36개를 상한으로 한다. 출혈·화상·둔화는 각각 적색·주황·청색의 얇은 발밑 링으로 구분한다. 피격 시 스프라이트를 0.12초 이내로 밝게 점멸하되 대규모 전투 가독성을 해치는 화면 흔들림은 사용하지 않는다.

우상단 일시정지 버튼은 전장 전체를 어둡게 하고 중앙 판타지 패널에 `전투 계속` 행동을 제공한다. 수동 정지, 레벨업 정지, 앱 background 정지는 서로의 재개 조건을 침범하지 않아야 한다.

우측:

- 소비 아이템, 후퇴, 특별 행동, 상태이상

전술 미니맵은 150×86 논리 픽셀 안에서 전장별 핵심 목표만 추상화한다. 성문 방어전은 북문·방어선·침투 압력을, 철수전은 탈출로·행렬 진행도·탈출 지점을 표시한다. 하단에는 `월광 야전 · 야행성` 또는 `잿바람 · 이동 -6%`처럼 현재 환경 효과를 한 줄로 고정 표기한다.

### S07-1. 전장 도감

- 캠프 좌측 `도감`에서 진입한다.
- 상단 필터는 전체 12, 일반 8, 정예 2, 지휘관 2를 항상 표시한다.
- 목록 카드는 세력색, 능력 아이콘, 이름, 세력/등급과 한 줄 전술 설명을 제공한다.
- 상세 패널은 전술 특성, 설정 문장, 체력/공격/방어/속도 보정과 희귀 전리품을 표시한다.
- 850px 미만에서는 목록 아래에 스크롤 가능한 상세 패널을 배치해 세로 넘침을 막는다.
- 일반 병사의 월드 능력 문양은 표시하지 않는다. 세력 tint와 병과 실루엣을 우선하고 정예/보스만 링과 문양으로 강조한다.

피드백 우선순위:

1. 플레이어 피격/위험
2. 목표물 위험
3. 궁극기 사용 가능
4. 레벨업
5. 사건 발생
6. 전리품/일반 처치

동시에 여러 모달을 띄우지 않는다. 레벨업과 사건이 겹치면 사건을 큐에 넣고 레벨업 완료 후 표시한다.

성문 월드 오브젝트 위에는 HUD와 동일한 HP 색상을 사용한 짧은 게이지와 금색 방어 목표 링을 배치한다. 공격/방어 구역은 저채도 적색·청색 틴트와 점선 전선으로만 구분해 중앙 전투 가독성을 유지한다.

목표 HUD 하단에는 아군/적군 지휘관 상태를 한 줄로 표시한다. 생존 중에는 회청색·적갈색 아이콘과 `지휘 유지/활동`을 사용하고, 전사·격퇴 시 금색 또는 저채도 경고색으로 상태 변화만 강조한다. 결과 화면에는 `아군 지휘관 생존`과 `적 지휘관 격퇴`를 전술 태그로 기록한다.

### S08. 레벨업 선택

- 전투를 정지하거나 10% 속도로 감속한다.
- 3개 카드를 동일 너비로 표시한다.
- 카드에는 아이콘, 이름, 현재→다음 단계, 효과 설명, 희귀도를 표시한다.
- 처음 획득하는 항목은 `NEW`, 보유 항목은 `LV.n → n+1`, 마지막 단계는 `MAX`로 표기한다.
- 최대 레벨과 중복 불가 항목은 선택 풀에서 제외한다.
- 재선택 기능이 있으면 남은 횟수와 비용을 명확히 표시한다.

하단 현재 빌드는 전투 시야를 보존하기 위해 최대 6개 원형 슬롯만 표시한다. 무기는 실제 아이콘, 패시브와 특성은 기능 아이콘을 사용하며 각 슬롯 아래에 `LV.n` 또는 `MAX`를 붙인다. 4개 무기보다 더 많은 성장 항목이 쌓이면 무기를 우선하고 나머지는 획득 순서대로 노출한다.

### S09. 전장 사건

- 상단에 등급, 중앙에 사건명과 이미지, 하단에 선택지를 배치한다.
- 각 선택지에 즉시 결과가 아니라 위험 방향과 보상 경향을 표시한다.
- 전설 사건은 조명과 사운드로 구분하되 텍스트 가독성을 유지한다.
- 강제 사건에는 선택지 대신 계속 버튼과 변화 내용을 표시한다.
- 모달이 열린 동안 Flame 전투와 전투 시계는 완전히 정지하며 중앙 전투는 어두운 scrim 아래 맥락으로 남긴다.
- 일반 회백색, 특수 청색, 희귀 보라색, 전설 금색의 얇은 테두리와 등급 라벨을 사용한다.
- 후퇴 선택지는 적갈색 위험 표시와 `보상 일부 보존` 문구로 즉시 종료임을 명확히 한다.
- 선택 후 모달은 닫히고 4초 결과 배너를 표시하며, 최종 결과 화면에는 등급·사건명·선택·결과를 한 줄씩 기록한다.

### S10. 전투 결과

표시 순서:

1. 승리/후퇴/패배 제목
2. 전투 시간, 처치, 목표, MVP
3. 골드와 경험치 증가
4. 전리품 등급별 공개
5. 발생 사건과 선택 결과
6. 귀환/재출전

희귀 전리품은 작은 지연과 광택을 사용하되 전체 결과 확인을 방해하지 않는다. 보상은 결과 화면 진입 시 한 번만 확정한다.

MVP 패널은 결과 요약 바로 아래에 금색 휘장, 전과명, 한 줄 근거와 최대 6개 전과 태그로 표시한다. 보상 명세는 계약/목표/전과/사건의 골드·경험치를 나란히 보여주고 `산출 → 회수`와 보존율을 한 문장으로 강조한다. 전리품 카드는 일반 회백색, 고급 녹색, 희귀 청색, 영웅 보라색, 전설 금색 테두리와 짧은 scale/fade 공개 효과를 사용한다. 720px 높이에서는 결과 패널 전체를 스크롤하되 귀환/재출전 버튼이 마지막에 명확히 노출되어야 한다.

영구 성장 패널은 보상 명세 바로 아래에 선택 용병과 착용 무기를 2열로 배치한다. 각 열은 이름, 영구 레벨/성장 단계, XP 게이지, 획득 XP를 표시하고 레벨이 오른 경우 금색 `LEVEL UP` 피드백을 추가한다. 저장 실패 시 적갈색 경고 패널을 표시하되 귀환과 재출전을 막지 않는다.

### S11. 용병 모집

연출 단계:

1. 계약서 선택/서명
2. 등급을 암시하는 봉인과 빛
3. 캐릭터 실루엣
4. JRPG 전신 일러스트 공개
5. 이름, 종족, 직업, 등급
6. 고유 특성과 고유무기 시너지
7. 명부 이동 또는 다시 모집

중복 획득은 변환 결과와 보유량을 즉시 설명한다. 확률 정보는 별도 접근 가능한 화면에 명확하게 제공한다.

알파 구현은 전용 16:9 루나 계약 배너 위에 계약서/크리스탈 보유량과 1회/10회 행동을 좌측에 배치한다. 계약 시 금색·보라색 봉인이 확대된 뒤 실제 획득 용병의 전신 카드로 전환한다. 10회 결과는 초상 그리드와 대표 공개 카드를 함께 사용하며 중복 증표 총량을 별도 보라색 패널로 강조한다.

### S12. 상점

- 탭: 일반, 전쟁, 길드, 명예, 암시장
- 카드: 상품 이미지, 이름, 수량, 가격, 남은 구매 횟수
- 갱신 시간과 갱신 비용을 상단 또는 하단 고정 영역에 표시
- 구매 직전에 상품, 수량, 재화, 구매 후 잔액을 확인한다.
- 재화 부족 시 관련 획득처로 이동할 수 있다.

알파에서는 일반/전쟁/명예 3개 탭만 활성화한다. 상품 카드에는 현재 보유량과 남은 구매 횟수를 함께 표시하며 구매 확인 Dialog에서 구매 후 잔액을 보여준다. 갱신은 모든 탭의 구매 한도를 초기화하므로 별도 확인 Dialog를 사용한다.

## 7. 오버레이와 피드백

### Toast

획득, 장착, 저장 완료 등 되돌릴 수 있는 작은 결과에 사용한다. 2초 이내 사라지며 연속 발생 시 큐로 처리한다.

### Dialog

후퇴, 분해, 희귀 재화 소비처럼 결과가 큰 행동에만 사용한다. 제목은 질문형보다 결과 중심 문구를 사용한다.

### Loading

- 300ms 미만 작업에는 로딩을 표시하지 않는다.
- 긴 자산 로딩은 세계관 문구와 진행률을 제공한다.
- 전투 진입 전 필요한 전장/스프라이트를 선로딩한다.

### Empty/Error

- 단순 빈 공간 대신 이유와 다음 행동을 제공한다.
- 오류 문구에 내부 예외나 코드만 표시하지 않는다.
- 저장 실패는 플레이를 막지 않되 재시도와 상태를 알려준다.

## 8. 접근성과 사용성

- 텍스트 대비 WCAG AA 수준을 가능한 범위에서 유지한다.
- 색만으로 희귀도, 진영, 선택 상태를 구분하지 않는다.
- 텍스트 크기 확대 시 핵심 버튼이 잘리지 않게 한다.
- 진동, 화면 흔들림, 섬광, 대미지 숫자, 파티클 밀도 옵션을 제공한다.
- 궁극기 강한 섬광은 밝기와 빈도를 제한한다.
- 모든 주요 메뉴 버튼에 의미 있는 semantics label을 제공한다.
- Web 키보드/포커스 이동과 모바일 뒤로가기 동작을 정의한다.

설정 화면은 2열 판타지 패널로 효과음, 진동, 화면 흔들림, 섬광 감소, 저사양, 큰 글자, 이동 조작, 자동 표적과 첫 계약 안내 재생을 제공한다. 700px 미만에서는 1열 스크롤로 전환한다. 이동은 혼합/가상 스틱/화면 터치, 표적은 거리/정예/목표 위협을 금색 선택 칩으로 표시한다. 모든 토글은 카드 전체로 조작 가능하고 semantics에 기능명과 상태를 함께 제공한다.

첫 계약 안내는 캠프를 어둡게 유지한 채 최대 470px 패널로 표시한다. 4단계 진행 수, 제목, 설명, 다음/건너뛰기 행동을 항상 제공하고 접근성에서는 별도 route로 읽는다.

### 장비 빌드 화면

- 좌측 상단에 방어구·장신구·전술 도구 3슬롯을 항상 표시하고 그 아래 무기 그리드를 유지한다.
- 슬롯을 누르면 동일 슬롯의 3개 이상 장비를 이름·등급·설명·전투 변화량과 함께 비교한다.
- 현재 장착 장비는 금색 테두리와 `장착 중` 텍스트를 함께 사용한다.
- 교체는 즉시 저장하며 강화 실패·파괴가 없다는 규칙을 다이얼로그에 명시한다.

## 9. UI QA 체크리스트

- 1280×720과 최소 지원 가로 화면에서 overflow가 없는가.
- SafeArea가 상단/하단 기능을 가리지 않는가.
- 중앙 전투 시야가 목표 비율 이상 확보되는가.
- 버튼 상태가 기본/hover/focus/pressed/disabled에서 구분되는가.
- 선택과 장착 상태를 색 외의 방법으로도 구분하는가.
- 긴 한글/영문 이름과 큰 재화 숫자가 잘리지 않는가.
- 로딩, 빈 상태, 오류 상태가 정의되어 있는가.
- 뒤로가기 후 이전 선택과 스크롤 상태가 합리적으로 보존되는가.
- 생성 이미지가 텍스트 대비와 크롭을 방해하지 않는가.
- 기본 Material 스타일이 노출되지 않는가.
