# 구현 작업 기획서

## 1. 목적

이 문서는 `PROJECT_PLAN.md`의 게임 기획을 Flutter + Flame에서 구현하기 위한 작업 단위, 의존성, 완료 조건, 테스트 기준을 정의한다. 작업 순서는 핵심 루프가 항상 실행 가능한 상태를 유지하도록 정한다.

## 2. 기술 기준

- Flutter 최신 stable: 메뉴, HUD, 오버레이, 접근성, 반응형 UI
- Flame 최신 stable: 전장 월드, 렌더링, 입력, 전투 시간과 시스템 업데이트
- Dart: 게임 데이터, 규칙, 저장 모델
- Web release: 기능별 주 테스트 대상
- iOS/Android: 마일스톤별 빌드 및 실제 기기 성능 확인
- Git: 하나의 검증 가능한 작업 단위당 하나의 명확한 커밋

## 3. 목표 구조

```text
lib/
├── app/
│   ├── app.dart
│   ├── navigation/
│   └── theme/
├── core/
│   ├── data/
│   ├── persistence/
│   ├── audio/
│   └── widgets/
├── features/
│   ├── camp/
│   ├── contracts/
│   ├── mercenaries/
│   ├── equipment/
│   ├── recruitment/
│   ├── shop/
│   └── results/
├── game/
│   ├── survivor_game.dart
│   ├── world/
│   ├── render/
│   ├── systems/
│   ├── spatial/
│   └── models/
└── main.dart
```

현재 `main.dart`, `battle_game.dart`, `game_data.dart`에서 동작하는 수직 슬라이스를 기능 단위로 분리하되, 분리 과정에서 플레이 가능한 상태를 깨뜨리지 않는다.

## 4. 시스템 경계와 데이터 흐름

| 영역 | 소유 상태 | 입력 | 출력 |
|---|---|---|---|
| Flutter App | 재화, 보유 용병, 장비, 계약, 영구 성장 | 사용자 메뉴 조작 | 전투 설정, 저장 데이터 |
| Battle Session | 런 레벨, HP, 전투 시간, 유닛, 사건 | 전투 설정, 포인터/가상 스틱 | 결과 보고서 |
| Content Repository | 용병, 무기, 적, 전장, 사건 정의 | ID 조회 | 불변 콘텐츠 데이터 |
| Save Repository | 계정 진행, 옵션, 마지막 장비 | 상태 변경 | 로컬 저장/복구 |
| Asset Registry | 이미지, 스프라이트, 오디오 경로 | 콘텐츠 ID | 로딩 가능한 자산 참조 |

Flutter가 `BattleConfig`를 만들어 Flame에 전달하고, Flame은 종료 시 `BattleReport`만 반환한다. Flame 내부 런 상태를 Flutter 위젯이 직접 수정하지 않는다.

## 5. 공통 데이터 모델

### BattleConfig

- contractId
- battlefieldId
- mercenaryId
- weaponId
- permanentStatsSnapshot
- difficultyModifiers
- deterministicSeed

### BattleReport

- result: victory / retreat / defeat
- elapsedTime
- playerKills / alliedKills
- objectivesCompleted
- gold / accountXp / mercenaryXp
- lootIdsAndAmounts
- triggeredEventIdsAndChoices
- finalRunBuild
- mvpRecord

### 콘텐츠 ID 규칙

`category_subject_variant`의 소문자 snake_case를 사용한다.

- `merc_luna_belhardt`
- `weapon_moon_blades`
- `enemy_kingdom_swordsman`
- `event_red_moon`
- `battlefield_north_gate`

저장 데이터는 표시 이름이 아니라 ID를 사용한다.

## 6. 에픽별 구현 작업

### E01. 프로젝트 구조 분리

작업:

- 테마, 공통 패널, 버튼, 통화 표시를 `core/widgets`로 이동
- 화면을 기능 폴더로 분리
- 전투 모델과 콘텐츠 모델의 Flutter 의존성을 최소화
- 정적 리스트를 repository 인터페이스 뒤로 이동
- 라우팅/화면 상태를 명시적인 AppState로 전환

완료 조건:

- 기존 전체 플레이 루프가 동일하게 동작한다.
- 기능 파일 하나가 500줄을 과도하게 넘지 않는다.
- 순환 import가 없다.
- 분석과 기존 위젯 테스트가 통과한다.

### E02. 입력과 플레이어 이동

작업:

- 모바일 가상 스틱과 Web 포인터 드래그 통합
- 입력 dead zone, 이동 가속/감속, 화면 경계 처리
- 용병별 이동속도와 대시 상태 반영
- 포커스 이탈/일시정지 시 입력 초기화

완료 조건:

- 터치가 끊겨도 캐릭터가 계속 이동하지 않는다.
- 화면 크기와 픽셀 비율에 따라 이동 감각이 달라지지 않는다.
- 조이스틱이 HUD 버튼 이벤트를 가로채지 않는다.

### E03. 전장 목표 시스템

작업:

- `ObjectiveSystem`과 objective 상태 머신 구현
- 생존, 방어, 호위, 암살, 점령 목표 타입
- 목표물 HP, 진행률, 성공/실패 조건
- HUD 목표 리스트와 월드 마커 연결
- 결과 보고서에 목표 전과 기록

성문 방어전 완료 조건:

- 성문 HP가 화면과 HUD에 표시된다.
- 적 병력이 플레이어가 아닌 성문도 공격한다.
- 제한 시간 또는 지휘관 처치로 승리한다.
- 성문 파괴 시 패배 결과로 이동한다.

### E04. 진영 AI와 적 역할

작업:

- 진영, 분대, 목표 우선순위 모델
- 보병/방패병/궁병/기병/마법병/공성병기 행동
- 근접/원거리 공격과 재사용 대기시간
- 지휘관 오라와 지휘관 사망 반응
- 화면 밖 낮은 빈도 업데이트

완료 조건:

- 모든 적이 플레이어만 추적하지 않는다.
- 역할별 실루엣, 사거리, 이동 패턴이 구분된다.
- 500유닛에서 공간 탐색이 프레임 예산을 넘지 않는다.

### E05. 무기와 투사체 시스템

작업:

- `WeaponSystem`, `ProjectilePool`, `DamageResolver`
- 8개 무기의 공격 패턴 구현
- 치명타, 관통, 연쇄, 범위, 출혈, 상태이상
- 무기 레벨별 수치/시각 변화
- 자동 대상 우선순위 설정

완료 조건:

- 무기 8개가 이름과 수치만 다른 형태가 아니다.
- 풀링된 투사체가 전투 종료 후 모두 반환된다.
- 무기별 DPS와 화면 점유율을 테스트 데이터로 기록한다.

M2.3 수직 슬라이스 구현은 8개 `WeaponPattern`, 순수 `DamageResolver`, 64개 투사체·96개 VFX·36개 대미지 숫자 풀을 사용한다. 무기별 정밀 DPS 표와 레벨별 화면 점유율은 M2.4 밸런스 데이터 작업에서 이어서 기록한다.

### E06. 런 레벨업과 빌드

작업:

- 경험치 오브젝트 또는 직접 지급 정책 결정
- 레벨업 선택 풀과 가중치
- 중복 선택 단계, 희귀도, 해금 조건
- 새 무기 획득, 무기 강화, 패시브/특성 강화
- 선택 전 미리보기와 현재 빌드 HUD

완료 조건:

- 유효하지 않은 선택이나 최대 레벨 선택이 나오지 않는다.
- 일시정지 중 전투 상태가 진행되지 않는다.
- 같은 시드에서 선택 풀 재현이 가능하다.

### E07. 전장 사건

작업:

- event definition, trigger evaluator, weighted picker
- 일반/특수/희귀/전설 등급 연출
- 선택형 사건의 시간 정지와 결과 적용
- 사건 중복 방지, 상호 배타 조건
- 8개 알파 사건 구현

완료 조건:

- 시드 기반 자동 테스트로 각 사건을 강제 재현할 수 있다.
- 전술적 후퇴가 보상 보존 규칙을 정확히 적용한다.
- 결과 화면에 발생 사건과 선택 결과가 기록된다.

### E08. 고유 궁극기

작업:

- 캐릭터/고유무기 조합 검증
- 게이지 획득과 발동 입력
- 시간 감속, 컷인, 화면 조명, 피해 단계 분리
- 루나/카일/세라 궁극기 구현
- 저사양 이펙트 축소 옵션

완료 조건:

- 고유무기가 아니면 궁극기가 활성화되지 않는다.
- 컷인 중 게임 상태와 피해 타이밍이 재현 가능하다.
- 3종 궁극기가 색과 모션만 다른 동일 공격이 아니다.

### E09. 결과와 보상

작업:

- 승리/후퇴/패배 결과 분기
- 처치, 목표, 사건, MVP, 최종 빌드 표시
- 전리품 공개 순서와 희귀도 연출
- 보상을 영구 상태에 원자적으로 반영
- 재출전 시 동일 계약/용병/장비 유지

완료 조건:

- 버튼 중복 입력으로 보상이 두 번 지급되지 않는다.
- 결과를 건너뛰어도 저장 상태가 일치한다.

### E10. 영구 성장과 저장

작업:

- save schema version과 migration
- 용병 경험치/레벨, 무기 레벨, 재화, 장착 정보
- 자동 저장 시점과 실패 복구
- 개발용 초기화/테스트 세이브
- Web local storage와 모바일 로컬 저장 구현

완료 조건:

- 앱 재시작 후 재화, 장착, 성장이 복원된다.
- 오래된 저장 버전을 마이그레이션한다.
- 손상된 저장은 백업 또는 안전한 기본 상태로 복구한다.

### E11. 캠프 메타 화면

작업:

- 용병 상세 탭: 정보, 레벨업, 장비, 스킬, 스토리
- 모집 연출과 계약 결과
- 일반/전쟁/길드/명예/암시장
- 임무, 도감, 대장간
- 일일 갱신 타이머와 알림 배지

완료 조건:

- 모든 화면이 실제 데이터와 핵심 루프에 연결된다.
- 미구현 버튼은 숨기거나 명확한 준비 중 상태를 제공한다.

### E12. 오디오와 피드백

작업:

- 캠프/지도/전투/결과 BGM 상태 전환
- 무기, 피격, 처치, 레벨업, 사건, 궁극기 SFX
- 진동 강도와 비활성화 옵션
- 동시 효과음 수 제한

완료 조건:

- 화면 전환 시 BGM 중복 재생이 없다.
- 대규모 교전에서도 소리가 깨지거나 과밀하지 않다.

## 7. 성능 구현 계획

### 프레임 예산

60fps 목표에서 한 프레임은 16.67ms다.

| 영역 | 목표 예산 |
|---|---:|
| AI/목표 선택 | 3.0ms |
| 이동/전투 계산 | 3.0ms |
| 렌더 준비 | 2.0ms |
| Canvas/Sprite 렌더 | 5.0ms |
| Flutter HUD/기타 | 2.0ms |
| 여유 | 1.67ms |

### 필수 기법

- 데이터 지향 유닛 배열과 배치 렌더링
- 공간 해시/Grid로 근접 질의 범위 제한
- 투사체, 파티클, 대미지 숫자 Object Pool
- 화면 밖 AI/애니메이션 tick 감소
- 고정 상한을 가진 파티클과 대미지 숫자
- 동일 텍스처 atlas와 sprite batching
- 거리별 단순화된 애니메이션/실루엣

### 벤치마크 단계

330 → 500 → 750 → 1,000 유닛으로 늘리며 평균, p95, 최악 프레임 시간을 기록한다. 단순 FPS 숫자만 기록하지 않고 AI, 렌더, GC allocation을 구분한다.

## 8. 테스트 전략

| 레벨 | 대상 | 도구/방법 |
|---|---|---|
| Unit | 피해 계산, 드롭, 사건 조건, 저장 migration | `flutter test` |
| Widget | 화면 전환, 선택, 장착, 결과 반영 | Flutter widget test |
| Game System | 고정 시드 전투, 목표, AI, 풀링 | Flame headless/unit test |
| Golden | 핵심 UI 상태와 반응형 레이아웃 | Flutter golden test |
| Browser | Web release 실제 입력과 렌더 | 1280×720, 모바일 가로 |
| Device | 성능, 터치, 발열, 메모리 | iOS/Android 실제 기기 |

## 9. 작업 단위 템플릿

모든 구현 이슈는 다음을 포함한다.

- 목적과 플레이어 가치
- 관련 기획/화면 문서 링크
- 포함 범위와 제외 범위
- 데이터/상태 변화
- UI 정상·빈 상태·오류 상태
- 성능 영향과 상한
- 자동 테스트와 수동 확인 목록
- 완료 조건

## 10. 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 전투 유닛 증가로 Web 프레임 저하 | 핵심 경험 훼손 | 배치 렌더, LOD, 벤치마크 조기 수행 |
| 생성 이미지 간 캐릭터 불일치 | 수집 가치 저하 | 캐릭터 식별 체크리스트와 모델 시트 고정 |
| `main.dart` 집중 | 변경 충돌과 테스트 어려움 | E01을 다음 구현의 선행 작업으로 수행 |
| 영구/런 상태 혼재 | 보상 중복과 저장 버그 | BattleConfig/Report 경계 강제 |
| UI 정보 과밀 | 전투 시야와 조작 방해 | HUD 안전 영역과 우선순위 토큰 준수 |
| 콘텐츠 추가가 코드 수정에 의존 | 확장 속도 저하 | ID 기반 repository와 데이터 파일 전환 |
